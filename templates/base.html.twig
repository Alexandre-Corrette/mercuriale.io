<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        {# ── PWA Meta Tags ── #}
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
        <meta name="theme-color" content="#1e3a5f">
        <meta name="apple-mobile-web-app-capable" content="yes">
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
        <meta name="apple-mobile-web-app-title" content="Mercuriale">
        <meta name="description" content="Gestion de mercuriale et contrôle des livraisons pour la restauration collective">
        <link rel="manifest" href="{{ asset('manifest.json') }}">
        <link rel="apple-touch-icon" href="{{ asset('icons/icon-192x192.png') }}">

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/tokens.css') }}">
            <link rel="stylesheet" href="{{ asset('css/components/button.css') }}">
            <link rel="stylesheet" href="{{ asset('css/components/card.css') }}">
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
            <link rel="stylesheet" href="{{ asset('css/push-notification.css') }}">
            <link rel="stylesheet" href="{{ asset('css/install-prompt.css') }}">
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <div data-controller="network-status">
            {# Bandeau offline #}
            <div data-network-status-target="banner"
                 class="hidden bg-amber-500 text-white px-4 py-2 text-center text-sm font-medium">
                <i class="fas fa-wifi-slash mr-2"></i>
                Mode hors connexion — Les photos seront envoyées automatiquement au retour du réseau
                <a href="/app/pending" class="ml-4 underline hover:no-underline">
                    Voir les BL en attente
                    <span data-network-status-target="pendingCount"
                          data-pending-wrapper
                          class="hidden ml-1 bg-white text-amber-600 px-2 py-0.5 rounded-full text-xs font-bold">
                        0
                    </span>
                </a>
            </div>

            {% block app_header %}
                {% if app.user %}
                <header class="app-header">
                    <a href="{{ path('admin') }}">
                        <img src="{{ asset('images/logo-mercuriale-rectangulaire.png') }}" alt="Mercuriale.io" class="app-header__logo">
                    </a>
                </header>
                {% endif %}
            {% endblock %}

            {% block body %}{% endblock %}
        </div>

        {# ── Push Notification Prompt (authenticated users only) ── #}
        {% if app.user %}
            <div data-controller="push-notification"
                 data-push-notification-vapid-key-value="{{ vapid_public_key() }}">
                <div data-push-notification-target="prompt" class="push-prompt">
                    <div class="push-prompt__inner">
                        <div class="push-prompt__icon">
                            <i class="fas fa-bell"></i>
                        </div>
                        <div class="push-prompt__text">
                            <p class="push-prompt__title">Notifications</p>
                            <p class="push-prompt__description">Recevez une alerte quand vos BL sont validés ou en cas d'anomalie.</p>
                        </div>
                        <div class="push-prompt__actions">
                            <button class="push-prompt__btn push-prompt__btn--dismiss"
                                    data-action="push-notification#dismiss">Plus tard</button>
                            <button class="push-prompt__btn push-prompt__btn--accept"
                                    data-action="push-notification#accept">Activer</button>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}

        {# ── A2HS Install Prompt (authenticated users only) ── #}
        {% if app.user %}
            <div data-controller="install-prompt">
                <div data-install-prompt-target="androidPrompt" class="install-prompt">
                    <div class="install-prompt__inner">
                        <div class="install-prompt__icon">
                            <i class="fas fa-download"></i>
                        </div>
                        <div class="install-prompt__text">
                            <p class="install-prompt__title">Installer Mercuriale</p>
                            <p class="install-prompt__description">Accédez rapidement à l'app depuis votre écran d'accueil.</p>
                        </div>
                        <div class="install-prompt__actions">
                            <button class="install-prompt__btn install-prompt__btn--dismiss"
                                    data-action="install-prompt#dismiss">Plus tard</button>
                            <button class="install-prompt__btn install-prompt__btn--install"
                                    data-action="install-prompt#install">Installer</button>
                        </div>
                    </div>
                </div>
                <div data-install-prompt-target="iosPrompt" class="install-prompt">
                    <div class="install-prompt__inner">
                        <div class="install-prompt__icon">
                            <i class="fas fa-download"></i>
                        </div>
                        <div class="install-prompt__text">
                            <p class="install-prompt__title">Installer Mercuriale</p>
                            <p class="install-prompt__ios-guide">
                                Appuyez sur <i class="fas fa-share-from-square"></i> Partager puis <strong>Sur l'écran d'accueil</strong>.
                            </p>
                        </div>
                        <div class="install-prompt__actions">
                            <button class="install-prompt__btn install-prompt__btn--dismiss"
                                    data-action="install-prompt#dismiss">Compris</button>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}

        {# ── PWA Service Worker Registration ── #}
        <script src="{{ asset('js/sw-register.js') }}"></script>
    </body>
</html>
